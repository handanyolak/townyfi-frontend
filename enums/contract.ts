export enum TownStatus {
  VOYAGE,
  SETTLE,
}

export enum NavigateDirection {
  Up,
  UpRight,
  Right,
  RightDown,
  Down,
  DownLeft,
  Left,
  LeftUp,
}

export enum Direction {
  Up,
  Right,
  Down,
  Left,
}

export enum Get {
  Health,
  Mana,
  Energy,
}
