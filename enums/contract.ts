export enum TownStatus {
  VOYAGE,
  SETTLE,
}

export enum TownMode {
  NORMAL,
  OFFENSIVE,
  DEFENSIVE,
}

export enum NavigateDirection {
  Up,
  UpRight,
  Right,
  RightDown,
  Down,
  DownLeft,
  Left,
  LeftUp,
}

export enum Direction {
  Up,
  Right,
  Down,
  Left,
}

export enum Get {
  Health,
  Mana,
  Energy,
}

export enum Prepare {
  Defend,
  Attack,
}
